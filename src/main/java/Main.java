import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        Divider calculator = new Divider(scanner.nextFloat(), scanner.nextFloat());
    }
}
